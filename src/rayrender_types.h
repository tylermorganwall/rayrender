#ifndef RAYRENDER_TYPES_H
#define RAYRENDER_TYPES_H

// Rcpp includes this package header before dependency plugin headers when
// generating RcppExports.cpp. Initialize Rcpp before RcppThread includes R's
// headers, including after a fresh Rcpp::compileAttributes() run.
#include <Rcpp.h>

#endif
