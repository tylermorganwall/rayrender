% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/media.R
\name{grid_medium}
\alias{grid_medium}
\title{Dense Grid Participating Medium}
\usage{
grid_medium(
  density,
  sigma_a = 0,
  sigma_s = 1,
  density_scale = 1,
  g = 0,
  bounds = rbind(c(-0.5, -0.5, -0.5), c(0.5, 0.5, 0.5)),
  emission = 0,
  temperature = NULL,
  emission_scale = 1,
  temperature_scale = 1,
  temperature_offset = 0,
  medium_transform = diag(4)
)
}
\arguments{
\item{density}{A nonnegative numeric array with dimensions `c(nx, ny, nz)`.}

\item{sigma_a}{Default `0`. Absorption coefficient, a nonnegative number or RGB
vector, per world-space distance unit.}

\item{sigma_s}{Default `1`. Scattering coefficient, a nonnegative number or RGB
vector, per world-space distance unit.}

\item{density_scale}{Default `1`. Nonnegative multiplier for both coefficients.}

\item{g}{Default `0`. Henyey-Greenstein asymmetry, strictly between -1 and 1.
Positive values scatter forward along the incident light direction.}

\item{bounds}{Default `rbind(c(-0.5, -0.5, -0.5), c(0.5, 0.5, 0.5))`.
Two rows giving minimum and maximum medium-space coordinates.}

\item{emission}{Default `0`. Scalar/RGB radiance or an array with dimensions
`c(nx, ny, nz, 3)`. The volume source is `sigma_a * Le`.}

\item{temperature}{Default `NULL`. Scalar kelvin temperature or an array with
the same dimensions as `density`. Mutually exclusive with nonzero emission.}

\item{emission_scale}{Default `1`. Nonnegative multiplier for emitted radiance.}

\item{temperature_scale}{Default `1`. Nonnegative multiplier applied after
subtracting `temperature_offset` from the temperature.}

\item{temperature_offset}{Default `0`. Offset subtracted from temperatures.}

\item{medium_transform}{Default `diag(4)`. Invertible affine matrix mapping
medium coordinates into the containing object's local coordinates.}
}
\value{
A reusable `ray_medium` description.
}
\description{
Density samples are cell centered and trilinearly interpolated. Array indices
correspond to x, y, z, with x varying fastest. Outside `bounds` the medium is
vacuum. Inside the bounds, interpolation clamps to the outermost samples.
}
\examples{
noise_field = ambient::noise_perlin(dim = c(300, 300, 300), frequency=0.02,octaves=8, gain=0.1)
noise_field[noise_field < 0] = 0
smoke = grid_medium(noise_field, g=0.2, sigma_a = 10)
scene = set_medium(cube(), smoke) |>
  add_object(sphere(x=3, material=light(intensity=10)))
render_scene(scene, integrator_type = "nee")
}
