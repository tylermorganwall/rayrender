% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disk_light.R
\name{disk_light}
\alias{disk_light}
\title{Uniform Infinite Disk Light}
\usage{
disk_light(
  color = "white",
  intensity = 1,
  angular_diameter = 0.53,
  direction = c(0, 1, 0),
  rotation = 0,
  name = "disk"
)
}
\arguments{
\item{color}{Default \code{"white"}. Light color, as an R color name, hexadecimal
string, or numeric RGB vector of three values between 0 and 1, as in \code{\link[=light]{light()}}.}

\item{intensity}{Default \code{1}. Nonnegative radiance multiplier for the color.}

\item{angular_diameter}{Default \code{0.53}. Full angular diameter in degrees,
greater than 0 and less than 180. Larger disks produce softer shadows and
more total illumination at the same intensity.}

\item{direction}{Default \code{c(0, 1, 0)}. Nonzero vector pointing from the scene
toward the disk center. Normalized automatically. World +Y is up, +Z is
north, and -X is east, matching \code{\link[=sun_light]{sun_light()}} and \code{\link[=moon_light]{moon_light()}}.}

\item{rotation}{Default \code{0}. Rotation in degrees around world Y, with the
same convention as \code{\link[=infinite_light]{infinite_light()}}.}

\item{name}{Default \code{"disk"}. Unique light name within the scene.}
}
\value{
A \code{ray_infinite_light} description.
}
\description{
Create a uniformly colored disk at infinite distance. Add it to a scene with
\code{\link[=add_infinite_light]{add_infinite_light()}} to illuminate surfaces and volumes and show the disk
in the background and reflections. No image files or sky datasets are needed.
}
\details{
The light has no distance falloff or parallax and adds no geometry.
Its color and radiance are constant across its angular extent. Intensity is
not normalized by disk area. The disk can be placed below the horizon;
it has no automatic horizon clipping or celestial atmospheric filtering.
It adds to other infinite lights without replacing automatic Sun or Moon disks.
Global \code{rotate_env} rotation applies in addition to its own rotation.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
scene = generate_ground(material = diffuse("grey60")) |>
  add_object(sphere(y = 1, material = diffuse("coral"))) |>
  add_infinite_light(disk_light(
    color = "#fff0dd", intensity = 100, angular_diameter = 10,
    direction = c(-1, 2, 1)
  ))
render_scene(scene, lookfrom = c(4, 3, -8), lookat = c(0, 1, 0),
             samples = 64, aperture = 0, ambient_light = FALSE)
\dontshow{\}) # examplesIf}
}
