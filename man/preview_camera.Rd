% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/camera.R
\name{preview_camera}
\alias{preview_camera}
\title{Preview Camera}
\usage{
preview_camera(
  scene,
  camera = NULL,
  width = 800,
  height = 800,
  fps = 24,
  samples = 1,
  ...
)
}
\arguments{
\item{scene}{Scene containing cameras.}

\item{camera}{Default `NULL`. Camera name or `ray_camera` object to preview.}

\item{width}{Default `800`. Preview width, in pixels.}

\item{height}{Default `800`. Preview height, in pixels.}

\item{fps}{Default `24`. Intended preview frame rate for animated cameras.}

\item{samples}{Default `1`. Number of samples per pixel for the preview.}

\item{...}{Additional arguments passed to `render_scene()`.}
}
\value{
Invisibly returns the rendered preview result.
}
\description{
Previews a scene-attached camera without writing image files.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
motion = generate_camera_motion(
  positions = list(c(0, 1, -10), c(6, 4, -8), c(0, 2, 8)),
  lookats = list(c(0, 0, 0), c(0, 0.5, 0), c(0, 0, 0)),
  fovs = c(35, 45, 30),
  frames = 24,
  type = "bezier"
)

scene = generate_ground() |>
  add_object(sphere()) |>
  add_camera(camera(name = "flythrough", motion = motion))

preview_camera(scene, camera = "flythrough", width = 400, height = 400)
\dontshow{\}) # examplesIf}
}
