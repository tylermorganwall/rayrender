% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen_line.R
\name{screen_line}
\alias{screen_line}
\title{Screen-space Lines}
\usage{
screen_line(
  x = 0,
  y = 0,
  z = 0,
  xend = 0,
  yend = 0,
  zend = 0,
  start = NULL,
  end = NULL,
  offset = c(0, 0),
  end_offset = offset,
  width = 2,
  color = "black",
  alpha = 1,
  lineend = "round",
  clip = TRUE,
  occlusion = FALSE,
  occlusion_mode = "anchor",
  occlusion_tolerance = 0.001
)
}
\arguments{
\item{x, y, z}{Default `0`. World-space coordinates of the line start point.}

\item{xend, yend, zend}{Default `0`. World-space coordinates of the line end
point.}

\item{start, end}{Default `NULL`. Optional 3-column matrix/data frame of
world-space start and end points. If supplied, overrides `x`, `y`, `z` and
`xend`, `yend`, `zend`.}

\item{offset}{Default `c(0,0)`. Pixel offset for the start point, as
`c(x,y)`, with positive y moving down the image.}

\item{end_offset}{Default `offset`. Pixel offset for the end point.}

\item{width}{Default `2`. Line width in pixels.}

\item{color}{Default `"black"`. Line color.}

\item{alpha}{Default `1`. Line alpha.}

\item{lineend}{Default `"round"`. Line end style. Options are `"round"`,
`"butt"`, and `"square"`.}

\item{clip}{Default `TRUE`. If `TRUE`, lines whose endpoints are both behind
the camera are skipped.}

\item{occlusion}{Default `FALSE`. If `TRUE`, use scene geometry to hide the
line.}

\item{occlusion_mode}{Default `"anchor"`. If `"anchor"`, occlusion skips the
entire line when its midpoint is blocked. If `"line"` or `"partial"`, each
line pixel is hidden only when the scene depth at that pixel is closer than
the interpolated screen-space line depth.}

\item{occlusion_tolerance}{Default `0.001`. Endpoint tolerance for occlusion.}
}
\value{
A data frame describing screen-space line annotations.
}
\description{
Creates line annotations for `render_scene()` that are anchored to 3D
world-space endpoints but drawn in 2D screen space after rendering.
}
