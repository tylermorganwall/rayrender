% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/camera.R
\name{get_camera}
\alias{get_camera}
\title{Get Camera}
\usage{
get_camera(scene, camera = NULL)
}
\arguments{
\item{scene}{Scene containing cameras.}

\item{camera}{Default `NULL`. Camera name, `ray_camera` object, or `NULL` to use the active camera.}
}
\value{
A `ray_camera` object.
}
\description{
Gets a camera from a `ray_scene`.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
scene = generate_ground(material=diffuse(color="grey20")) |>
  add_camera(camera(name = "wide", fov = 55), active = FALSE) |>
  add_camera(camera(name = "main", fov = 35), active = TRUE)

# Returns the active camera.
get_camera(scene)

# Returns a named camera.
get_camera(scene, "wide")
\dontshow{\}) # examplesIf}
}
