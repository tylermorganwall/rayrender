% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/camera.R
\name{set_active_camera}
\alias{set_active_camera}
\title{Set Active Camera}
\usage{
set_active_camera(scene, camera)
}
\arguments{
\item{scene}{Scene containing cameras.}

\item{camera}{Camera name.}
}
\value{
A modified `ray_scene`.
}
\description{
Sets the active camera for a `ray_scene`.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
scene = generate_ground() |>
  add_camera(camera(name = "wide", fov = 55), active = FALSE) |>
  add_camera(camera(name = "detail", fov = 20), active = FALSE)

scene = set_active_camera(scene, "detail")
get_camera(scene)
\dontshow{\}) # examplesIf}
}
