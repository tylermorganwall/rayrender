% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloud.R
\name{cloud}
\alias{cloud}
\title{Procedural Cloud Object}
\usage{
cloud(
  x = 0,
  y = 0,
  z = 0,
  width = 100,
  height = 25,
  depth = 75,
  style = c("cumulus", "stratus"),
  seed = 42,
  resolution = 128,
  coverage = 0.5,
  detail = 0.35,
  optical_depth = 8,
  g = 0.65,
  angle = c(0, 0, 0),
  order_rotation = c(1, 2, 3),
  scale = c(1, 1, 1)
)
}
\arguments{
\item{x}{Default \code{0}. x-coordinate of the center of the cloud's bounding box.}

\item{y}{Default \code{0}. y-coordinate of the center of the cloud's bounding box.}

\item{z}{Default \code{0}. z-coordinate of the center of the cloud's bounding box.}

\item{width}{Default \code{100}. Width of the cloud volume along its local x-axis.}

\item{height}{Default \code{25}. Height of the cloud volume along its local y-axis.}

\item{depth}{Default \code{75}. Depth of the cloud volume along its local z-axis.}

\item{style}{Default \code{c("cumulus", "stratus")}. Cloud shape. \code{"cumulus"} creates
rounded bodies with billowing tops; \code{"stratus"} creates a shallow cloud bank.}

\item{seed}{Default \code{42}. Nonnegative integer seed for the cloud shape.
Generating a cloud preserves the caller's random-number state.}

\item{resolution}{Default \code{128}. Integer number of density cells along the
longest dimension, at least 24. Other dimensions follow the aspect ratio,
with at least eight cells each. Higher values add detail and use more memory.}

\item{coverage}{Default \code{0.5}. Number between zero and one controlling the
size and connection of cloud bodies. Zero does not make the volume empty;
use \code{optical_depth = 0} for a non-scattering cloud.}

\item{detail}{Default \code{0.35}. Number between zero and one controlling the
strength of small-scale Perlin detail.}

\item{optical_depth}{Default \code{8}. Nonnegative extinction through a fully
dense column of length \code{height}. Actual optical depth depends on the density
along the ray. Extinction is 99.9\% scattering and 0.1\% absorption.}

\item{g}{Default \code{0.65}. Henyey-Greenstein scattering asymmetry, strictly
between -1 and 1. Positive values scatter forward along the light direction.}

\item{angle}{Default \code{c(0, 0, 0)}. Rotation in degrees around the x, y, and z
axes, applied in the order specified by \code{order_rotation}.}

\item{order_rotation}{Default \code{c(1, 2, 3)}. Order of rotations, referring to
x, y, and z. Must be a permutation of \code{c(1, 2, 3)}.}

\item{scale}{Default \code{c(1, 1, 1)}. Nonzero scale factors along x, y, and z.
A single value scales uniformly. Scales the density field and its boundary
together, retaining extinction per world-space unit.}
}
\value{
A single-row \code{ray_scene} containing an invisible box with an attached
cloud density grid.
}
\description{
Create a cloud volume with billowing Perlin-noise density. Add it to a scene
with \code{\link[=add_object]{add_object()}} and render with \code{integrator_type = "nee"}. Requires the
suggested package \code{ambient} to generate the density field.
}
\details{
The local bounding box is centered at zero before object transforms,
spanning \code{-c(width, height, depth) / 2} to \code{c(width, height, depth) / 2}.
The density fades to vacuum at all six faces; the box has no visible surface.
An unrotated, unscaled cloud with base altitude \code{b} has \code{y = b + height / 2}.
The center describes the box, not the irregular density's center of mass.

Positions and dimensions use scene units. Setting the dimensions rebuilds
the field and normalizes extinction by \code{height}. Applying \code{scale} stretches
the existing volume without renormalizing extinction, so stretching it along
a ray increases that ray's optical depth. Standard \code{\link[=group_objects]{group_objects()}},
\code{\link[=animate_objects]{animate_objects()}}, and \code{\link[=create_instances]{create_instances()}} operations transform the cloud
using the same object machinery as other closed shapes.

Cloud scattering is separate from \code{\link[=sky_light]{sky_light()}}'s clear-air atmospheric haze.
Avoid intersecting, non-nested cloud boxes, including their empty edge cells:
these are separate medium boundaries. Use one larger field for a connected
bank. The attached \code{\link[=grid_medium]{grid_medium()}} is stored in
\code{object$shape_info[[1]]$medium} for further density or scattering adjustments.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
if (requireNamespace("ambient", quietly = TRUE)) {
  # The default cloud is centered at the origin. Raise a cloud by its center
  # to put its base above the ground, then rotate the entire density field.
  puff = cloud(y = 20, width = 60, height = 20, depth = 40,
               angle = c(0, 25, 0), resolution = 64, optical_depth = 4)
  scene = generate_ground(material = diffuse("#699447")) |>
    add_object(puff) |>
    add_object(sphere(x = -50, y = 80, z = -30, radius = 15,
                      material = light(intensity = 40)))
  render_scene(scene, lookfrom = c(80, 35, -100), lookat = c(0, 20, 0),
               fov = 35, integrator_type = "nee", samples = 64,
               clamp_value = Inf, aperture = 0)

  # Reuse the shape at another position, or change the style and its detail.
  bank = cloud(x = 100, y = 30, z = 20, style = "stratus", seed = 17,
               width = 80, height = 10, depth = 50,
               coverage = 0.7, detail = 0.2, optical_depth = 6, g = 0.6)
  scaled = cloud(scale = c(1.5, 1, 0.75), angle = c(10, 30, 0),
                 order_rotation = c(2, 1, 3), resolution = 64)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=sky_light]{sky_light()}}, \code{\link[=grid_medium]{grid_medium()}}, \code{\link[=set_medium]{set_medium()}}
}
