% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sky_light_image.R
\name{sky_light_image}
\alias{sky_light_image}
\title{Image-Based Location and Time Sky Light}
\usage{
sky_light_image(
  lat,
  long,
  datetime,
  intensity = 1,
  rotation = 0,
  name = "sky",
  altitude = 0,
  visibility = 131.8,
  albedo = 0.5,
  resolution = 2048,
  hosek = TRUE,
  render_mode = "all",
  turbidity = 3,
  wide_spectrum = FALSE,
  below_horizon = TRUE,
  prague_rgb_correction = TRUE,
  prague_rgb_correction_strength = 1,
  prague_rgb_correction_gain = "auto",
  stars = FALSE,
  star_width = 1,
  stars_exposure = 0,
  planets = FALSE,
  moon = TRUE,
  moon_atmosphere = FALSE,
  moon_hosek = TRUE,
  exr_adopted_white = "D60",
  exr_metadata = TRUE,
  number_cores = 1,
  verbose = FALSE,
  sun = TRUE,
  environment_light_bake_white = FALSE,
  environment_light_bake_white_target = "D65",
  ...
)
}
\arguments{
\item{lat}{Latitude in degrees, between -90 and 90.}

\item{long}{Longitude in degrees, between -180 and 180.}

\item{datetime}{A single \code{POSIXct} date and time. Specify its time zone when
constructing it with \code{as.POSIXct()}.}

\item{intensity}{Default \code{1}. Nonnegative multiplier for this light's radiance.}

\item{rotation}{Default \code{0}. Additional rotation in degrees around the world
Y axis, using the same convention as \code{\link[=infinite_light]{infinite_light()}}.}

\item{name}{Default \code{"sky"}. Unique light name within the scene.}

\item{altitude}{Default \code{0}. Observer altitude in meters above sea level for
the entire sky image. Prague supports 0--15000 m with its full-altitude data.}

\item{visibility}{Default \code{131.8}. Prague meteorological visibility in kilometers,
from 20 to 131.8. Smaller values produce stronger haze.}

\item{albedo}{Default \code{0.5}. Uniform ground reflectance for the sky model,
between 0 and 1. Local surface materials are specified separately.}

\item{resolution}{Default \code{2048}. Height of the cached image in pixels;
its width is twice the height.}

\item{hosek}{Default \code{TRUE}. Generate a Hosek sky. Set \code{FALSE} to use Prague.}

\item{render_mode}{Default \code{"all"}. Select sky and Sun (\code{"all"}), sky without
the solar disk (\code{"atmosphere"}), or the solar disk alone (\code{"sun"}).
Moon, star, and planet switches are independent of this selection.}

\item{turbidity}{Default \code{3}. Hosek turbidity, from 1.7 to 10.}

\item{wide_spectrum}{Default \code{FALSE}. Use Prague's 55-channel sea-level data.}

\item{below_horizon}{Default \code{TRUE}. Include atmospheric radiance below the horizon.}

\item{prague_rgb_correction}{Default \code{TRUE}. Apply skymodelr's Prague RGB
tint correction.}

\item{prague_rgb_correction_strength}{Default \code{1}. Strength of the Prague RGB
tint correction. Must be finite and nonnegative: 0 disables correction,
and 1 applies the full calibrated correction.}

\item{prague_rgb_correction_gain}{Default \code{"auto"}. Calibrated Prague RGB
gains, or a numeric vector of three finite, positive linear RGB multipliers.}

\item{stars}{Default \code{FALSE}. Composite stars into the sky image.}

\item{star_width}{Default \code{1}. Stellar point-spread size, passed to
\code{skymodelr::generate_stars()}.}

\item{stars_exposure}{Default \code{0}. Artistic exposure adjustment for stars, in stops.}

\item{planets}{Default \code{FALSE}. Composite bright planets into the sky image.}

\item{moon}{Default \code{TRUE}. Composite a Moon image into the sky.
Set \code{FALSE} when adding a separate \code{\link[=moon_light]{moon_light()}}.}

\item{moon_atmosphere}{Default \code{FALSE}. Include atmospheric scattering of moonlight.}

\item{moon_hosek}{Default \code{TRUE}. Use Hosek for moonlight scattering.
Set \code{FALSE} to use Prague.}

\item{exr_adopted_white}{Default \code{"D60"}. Adopted white for EXR metadata:
\code{"D60"}, \code{"D65"}, or numeric XYZ with Y = 1. Does not change image pixels.}

\item{exr_metadata}{Default \code{TRUE}. Attach skymodelr color metadata to the EXR.}

\item{number_cores}{Default \code{1}. CPU threads used to generate the cached image.}

\item{verbose}{Default \code{FALSE}. Print sky-generation progress information.}

\item{sun}{Default \code{TRUE}. Include the solar disk when selected by \code{render_mode}.
Set \code{FALSE} to omit it. Hosek uses a separately sampled celestial disk.}

\item{environment_light_bake_white}{Default \code{FALSE}. Bake chromatic adaptation
from the generated EXR's \code{white_current} metadata into its RGB pixels before
using it for lighting. Requires \code{exr_metadata = TRUE}. The adapted image is
cached and reused for still images and animations.}

\item{environment_light_bake_white_target}{Default \code{"D65"}. Target white point
when baking: \code{"D50"}, \code{"D55"}, \code{"D60"}, \code{"D65"}, \code{"D75"}, \code{"E"}, or a finite
numeric XYZ vector with positive Y (normalized to Y = 1). Unlike
\code{exr_adopted_white}, this changes the image pixels when baking is enabled.}

\item{...}{Additional named arguments forwarded by
\code{skymodelr::generate_sky_latlong()} to its star, planet, and Moon generators.
Pass settings directly; location, datetime, and the cached filename are
managed by this light. Native atmospheric controls belong to \code{\link[=sky_light]{sky_light()}}.}
}
\value{
A \code{ray_infinite_light} containing a cached-image sky description.
}
\description{
Generate a cached sky EXR with \code{skymodelr::generate_sky_latlong()} and use it
as an infinite light. Add it to a scene with \code{\link[=add_infinite_light]{add_infinite_light()}}. The image
represents one observer and illuminates every scene position with the same
sky. Use \code{\link[=sky_light]{sky_light()}} for altitude-dependent lighting and finite-distance haze.
Both constructors include Sun and Moon by default, controlled with \code{sun} and
\code{moon}, and support optional stars and planets.
}
\details{
Image generation happens before rendering and is cached for the R
session. Changing only \code{intensity}, \code{rotation}, or \code{name} reuses the image.
Changing the baked target white point reuses the generated sky and caches a
separate adapted image. White-balance controls belong to this light; pass the
light to \code{\link[=add_infinite_light]{add_infinite_light()}} before calling \code{\link[=render_scene]{render_scene()}} or
\code{\link[=render_animation]{render_animation()}}.
Install any required Prague data with \code{skymodelr::download_sky_data()} first;
rendering does not download datasets.

With zero rotation, north is world +Z and east is world -X. Date, time, and
observer altitude remain fixed throughout the image. This light supports the
same integrators as \code{\link[=infinite_light]{infinite_light()}} and adds no finite atmospheric haze.
Use \code{\link[=render_scene]{render_scene()}}'s \code{iso} to adjust exposure.

Hosek skies automatically use a separately sampled Sun with matching turbidity,
intensity, rotation, and white adaptation. This keeps direct light independent
of image resolution. An explicit \code{\link[=sun_light]{sun_light()}} replaces that automatic disk.
For a separately sampled Sun with a Prague image, set \code{render_mode = "atmosphere"} and add
\code{\link[=sun_light]{sun_light()}} with matching location, time, and atmospheric settings. This
avoids relying on the environment image to resolve the small solar disk.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
time = as.POSIXct("2026-06-21 18:00:00", tz = "America/New_York")
scene = sphere(material = diffuse("white")) |>
  add_object(generate_ground())

scene |>
  add_infinite_light(sky_light_image(40.7, -74, time)) |>
  render_scene(
    lookfrom = c(0, 1, 10),
    lookat = c(0, 0, 0),
    width = 600,
    height = 300,
    samples = 32,
    iso = 3
  )

# A Prague image sky with an independently sampled solar disk.
# Install Prague data with skymodelr::download_sky_data() before rendering.
scene |>
  add_infinite_light(sky_light_image(
    40.7,
    -74,
    time,
    hosek = FALSE,
    render_mode = "atmosphere",
    altitude = 0,
    visibility = 50,
    albedo = 0.3
  )) |>
  add_infinite_light(sun_light(
    40.7,
    -74,
    time,
    sky_args = list(altitude = 0, visibility = 50, albedo = 0.3)
  )) |>
render_scene(
  lookfrom = c(0, 1, 10),
  lookat = c(0, 0, 0),
  aperture = 0,
  width = 600,
  height = 300,
  samples = 32,
  iso = 3
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=sky_light]{sky_light()}}, \code{\link[=infinite_light]{infinite_light()}}, \code{\link[=sun_light]{sun_light()}}, \code{\link[=moon_light]{moon_light()}}
}
