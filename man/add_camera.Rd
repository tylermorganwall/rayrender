% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/camera.R
\name{add_camera}
\alias{add_camera}
\title{Add Camera}
\usage{
add_camera(scene, camera, name = NULL, active = TRUE, replace = FALSE)
}
\arguments{
\item{scene}{Scene to modify.}

\item{camera}{Camera created with `camera()`.}

\item{name}{Default `NULL`. Optional name that overrides `camera$name`.}

\item{active}{Default `TRUE`. Whether to set this camera as the active scene camera.}

\item{replace}{Default `FALSE`. Whether to replace an existing camera with the same name.}
}
\value{
A modified `ray_scene`.
}
\description{
Adds a camera to a `ray_scene`.
}
\examples{
\dontshow{if (interactive() || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
scene = generate_ground(material=diffuse(color="grey20")) |>
  add_object(sphere()) |>
  add_camera(camera(
    name = "main",
    lookfrom = c(0, 1, -10),
    lookat = c(0, 0, 0),
    fov = 35
  ))

render_scene(scene, samples = 16)
\dontshow{\}) # examplesIf}
}
